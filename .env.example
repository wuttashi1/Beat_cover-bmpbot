BOT_TOKEN=
ADMIN_USER_ID=
PUBLISH_CHANNEL=
# Optional username check for the administrator (without @)
ADMIN_USERNAME=
# Optional persistent paths; defaults preserve existing deployment paths.
# BOT_DB_PATH=/data/users.db
# BPM_PRESETS_DIR=/data/presets

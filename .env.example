BOT_TOKEN=
ADMIN_USER_ID=
PUBLISH_CHANNEL=
# Optional display metadata; owner access uses ADMIN_USER_ID only
ADMIN_USERNAME=
# compose.yaml sets these paths automatically. For a manual deployment use:
# BOT_DB_PATH=/data/users.db
# BPM_PRESETS_DIR=/data/presets
